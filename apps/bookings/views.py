"""
No views yet. booking-payments owns the booking flow — availability
lookup, slot selection, payment initiation and confirmation — and will
add the views/htmx endpoints that create and update `Booking` records
here. BookingPage itself is served by Wagtail's normal page-serving
mechanism.
"""
